#!/usr/bin/env bash
set -euo pipefail

echo "Model download is intentionally disabled during Phase 1." >&2
echo "Select a winner using docs/benchmark-plan.md, then replace this guard with a public, credential-free, idempotent download." >&2
exit 2
